# ── Email (SMTP) ─────────────────────────────────────────────
# Works with Gmail, Outlook, or any SMTP provider (e.g. Mailgun, Brevo)

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false          # true only for port 465

SMTP_USER=your@gmail.com
SMTP_PASS=your-app-password   # Gmail: use an App Password, not your real password

# Where to send the notification email
NOTIFY_EMAIL=agency@valente-paris.com
